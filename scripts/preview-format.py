#!/usr/bin/env python3
"""
快速预览飞书文档排版效果
"""

import json
from pathlib import Path

OUTPUT_DIR = Path("/root/.openclaw/workspace/wechat-output")
OUTPUT_DIR.mkdir(exist_ok=True)

# 从 list_blocks 结果中提取内容
blocks_data = {
  "title": "【论文深读】RoadRunner：面向高速越野自动驾驶的自监督可通行性学习框架-TFR2025",
  "blocks": []
}

# 简化的内容提取
content = """# 【论文深读】RoadRunner：面向高速越野自动驾驶的自监督可通行性学习框架-TFR2025

## 论文示例：

在自动驾驶研究逐渐从"城市道路"走向"复杂自然环境"的今天，一个核心问题变得愈发关键：

**👉 机器人如何在"没有道路"的地方安全行驶？**

相比结构化道路，越野环境带来了本质挑战：
- 无车道线、无规则边界
- 地形复杂（坡度、碎石、植被）
- 感知不完整（遮挡、远距离稀疏）

在这样的背景下，来自 NASA JPL 与 ETH Zurich 的最新研究提出了一种新的解决范式：

**👉 RoadRunner：直接学习"可通行性"的端到端感知模型**

---

## 🧭 一、问题重构：从"语义理解"到"行动可行性"

传统自动驾驶感知 pipeline 通常是：

**感知 → 语义理解 → 规则推理 → 可通行性判断**

例如：
- 识别"草地 / 石头 / 树木"
- 再根据规则判断是否可行驶

但论文指出，这种方法存在结构性缺陷：
- ❌ 语义类别与可通行性并非一一对应
- ❌ 规则依赖人工设计，难以泛化
- ❌ 高速场景下 LiDAR 稀疏，语义信息不足

---

## ✅ RoadRunner 的核心思想

**👉 绕开语义中间层，直接预测 Traversability（可通行性）与 Elevation（地形高度）**

这本质上是一次**任务驱动的感知范式转变（Task-oriented Perception）**：

> 不再问"这是什么"，而是直接回答"能不能走"。

---

## 🧠 二、方法框架：多模态 + 自监督 + BEV 统一建模

### 1️⃣ 多模态融合：Camera + LiDAR → BEV 表示

RoadRunner 融合：
- 多摄像头图像
- 多 LiDAR 点云

并统一投影到 Bird's Eye View（BEV）空间，构建结构化环境表示

📌 技术关键：
- 图像 backbone（如 EfficientNet）
- 点云编码（PointPillars）
- Lift-Splat-Shoot 风格跨视角映射

最终通过 CNN 解码输出：

**👉 可通行性地图 + 高度地图**

---

### 2️⃣ 自监督学习：Hindsight Traversability Learning

论文最具创新性的部分在于监督信号的构建方式。

#### 🚩 核心机制：利用"未来信息"生成标签

具体流程：
1. 车辆在环境中真实行驶
2. 收集过去 + 未来 60s 内的传感器数据
3. 融合生成更完整的地形与风险评估
4. 作为"伪真值"用于训练

📌 本质：

**👉 用"未来观察"纠正"当前不确定性"**

📌 优势分析
- 无需人工标注
- 标签更接近真实物理可行性
- 可扩展到大规模数据

同时，这种方法相比传统自监督更进一步：

**👉 不仅学习表示，还学习"决策相关变量"**

---

### 3️⃣ 双任务学习：Traversability + Elevation

模型同时预测：
- Traversability（风险 / 可通行性）
- Elevation（连续地形高度）

论文指出：

**👉 两者并非完全互补，需要独立解码器**

这说明：
- 几何理解 ≠ 行为决策
- 多任务学习需考虑任务差异性

---

### 4️⃣ 面向部署的系统设计

RoadRunner 不仅是一个模型，更是一个**可部署系统模块**：

- 输出：100m × 100m BEV 地图
- 分辨率：20cm
- 延迟：≈ 140ms（相比 500ms 降低约 4 倍）

**👉 支持最高约 20 m/s 高速越野**

---

## 📊 三、数据集与实验设计

### 数据集构建

- 总行驶距离：16.5 km
- 样本数量：21,158
- 场景类型：
  - 森林
  - 峡谷
  - 沙地
  - 碎石路

📌 特点：
- 强非结构化
- 多遮挡
- 光照变化剧烈

---

### 评估指标

- Traversability：MSE / WMSE
- Elevation：MAE / WMAE
- Hazard Detection：Precision / Recall / F1

---

### 性能对比：RoadRunner vs X-Racer

结果表明：

**👉 RoadRunner 在多个维度全面优于传统系统**

- Traversability 误差显著降低（MSE ↓）
- Elevation 误差降低约 36%
- 风险检测精度大幅提升

---

### 关键发现（非常重要）

#### ✅ 在"未观测区域"仍具优势

在以下区域表现突出：
- LiDAR 未覆盖区域
- 远距离区域
- 遮挡区域

**👉 模型能够"推断未知"**

---

#### ⚠️ 泛化问题仍存在

论文指出：
- 测试集（不同区域）误差明显上升
- 存在 domain shift 问题

**👉 数据规模仍是瓶颈**

---

## ⚠️ 四、局限性与问题反思

论文给出了多个值得深入研究的问题：

### 1️⃣ 伪标签噪声问题
- ground truth 来自模型融合
- 存在误差传播

### 2️⃣ 动态障碍处理不足
- 对移动物体（车辆、动物）建模有限

### 3️⃣ 未观测区域高度估计不稳定
- 本质是"基于上下文猜测"

### 4️⃣ 数据规模与泛化能力限制
- 小规模数据难以支撑复杂分布

---

## 🔭 五、科研启发与研究方向

这篇论文最重要的价值，在于它提出了一种**新的研究范式**：

---

### 💡 1. Task-oriented Perception（任务导向感知）

从：

**👉 语义理解**

转向：

**👉 行为决策相关变量建模**

---

### 💡 2. 自监督 + 物理一致性学习

利用真实交互数据：

**👉 学习"可行动空间"**

---

### 💡 3. 多模态 BEV 仍是核心方向

融合：
- 图像（语义）
- 点云（几何）

**👉 构建统一空间表达**

---

### 💡 4. 潜在研究方向

- 不确定性建模（uncertainty-aware traversability）
- 时序建模（temporal fusion / memory）
- world model（环境动态建模）
- 感知 - 规划一体化（end-to-end driving）

---

## 🎓 六、总结

**👉 RoadRunner 的真正贡献，不只是性能提升，而是范式转变。**

它回答了一个关键问题：

> 在没有规则、没有标注、没有结构的世界里，
> 机器人如何学会"判断哪里能走"？

---

### 📌 一句话总结

**👉 从"识别世界"，到"理解可行动性"，RoadRunner 迈出了关键一步。**

---

📖 获取论文 / 代码 / 复现资料

欢迎留言交流

---

**✨ 持续关注，我们将带来更多自动驾驶与机器人前沿论文解读**

**✨欢迎对越野机器人感兴趣的同行加微信交流：15711463195**
"""

# 保存为 Markdown 文件
md_file = OUTPUT_DIR / "preview.md"
md_file.write_text(content, encoding='utf-8')

print(f"✅ 预览文件已保存：{md_file}")
print(f"\n📄 标题：{blocks_data['title']}")
print(f"📝 字数：{len(content)} 字符")
print(f"\n🔍 内容特点：")
print(f"  - 包含 {content.count('##')} 个一级章节")
print(f"  - 包含 {content.count('###')} 个二级章节")
print(f"  - 包含 {content.count('**')} 处加粗强调")
print(f"  - 包含 {content.count('- ')} 个列表项")
print(f"  - 包含 {content.count('> ')} 个引用块")
print(f"\n💡 提示：文档中的飞书图片在发布时会自动保留")
